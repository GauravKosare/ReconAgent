# URL of the ReconAgent backend. Local dev default is http://localhost:8000.
# In Vercel set this to your deployed backend, e.g. https://reconagent-api.onrender.com
# (no trailing slash). If unset or unreachable, the dashboard runs in demo mode.
NEXT_PUBLIC_API_URL=http://localhost:8000
